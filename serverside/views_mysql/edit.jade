doctype html
html
    head
        meta(charset="utf-8")
    body
        h1 
            a(href="/topic") Server Side JAVASCRIPT
        ul
            each topic in topics
                li
                    a(href="/topic/"+topic.no)= topic.title
        aricle 
          form(action="/topic/"+topic.no+"/edit" method="post")
            P 
              input(type="text",name="title", value =topic.title ,placeholder="title" )
            P
                textarea(name="description")
                  =topic.memo
            P
                input(type="submit")
        ul
            li
                a(href="/topic/add") write
